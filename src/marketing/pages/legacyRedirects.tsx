import { Navigate } from 'react-router-dom';

export function AiCallerRedirect() {
  return <Navigate to="/features" replace />;
}

export function AiChatbotRedirect() {
  return <Navigate to="/features" replace />;
}
